{
  "description": "autorun v0.8 - Gemini CLI native compatibility hooks",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [{
          "name": "autorun-init",
          "type": "command",
          "command": "uv run --quiet --project ${extensionPath} python ${extensionPath}/hooks/hook_entry.py --cli gemini",
          "timeout": 5000
        }]
      }
    ],
    "BeforeAgent": [
      {
        "matcher": "/afs|/afa|/afj|/afst|/autorun|/autostop|/estop|/ar:",
        "hooks": [{
          "name": "autorun-command",
          "type": "command",
          "command": "uv run --quiet --project ${extensionPath} python ${extensionPath}/hooks/hook_entry.py --cli gemini",
          "timeout": 5000
        }]
      }
    ],
    "BeforeTool": [
      {
        "matcher": "write_file|run_shell_command|replace|read_file|glob|grep_search|exit_plan_mode",
        "hooks": [{
          "name": "autorun-pretool",
          "type": "command",
          "command": "uv run --quiet --project ${extensionPath} python ${extensionPath}/hooks/hook_entry.py --cli gemini",
          "timeout": 5000
        }]
      }
    ],
    "AfterAgent": [
      {
        "matcher": "AUTORUN|/ar:",
        "hooks": [{
          "name": "autorun-afteragent",
          "type": "command",
          "command": "uv run --quiet --project ${extensionPath} python ${extensionPath}/hooks/hook_entry.py --cli gemini",
          "timeout": 5000
        }]
      }
    ],
    "AfterModel": [
      {
        "hooks": [{
          "name": "autorun-aftermodel",
          "type": "command",
          "command": "uv run --quiet --project ${extensionPath} python ${extensionPath}/hooks/hook_entry.py --cli gemini",
          "timeout": 5000
        }]
      }
    ],
    "AfterTool": [
      {
        "matcher": "write_file|replace|read_file|exit_plan_mode",
        "hooks": [{
          "name": "autorun-posttool-plan",
          "type": "command",
          "command": "uv run --quiet --project ${extensionPath} python ${extensionPath}/hooks/hook_entry.py --cli gemini",
          "timeout": 5000
        }]
      },
      {
        "matcher": "write_todos",
        "hooks": [{
          "name": "autorun-posttool-task",
          "type": "command",
          "command": "uv run --quiet --project ${extensionPath} python ${extensionPath}/hooks/hook_entry.py --cli gemini",
          "timeout": 5000
        }]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [{
          "name": "autorun-cleanup",
          "type": "command",
          "command": "uv run --quiet --project ${extensionPath} python ${extensionPath}/hooks/hook_entry.py --cli gemini",
          "timeout": 5000
        }]
      }
    ]
  }
}
