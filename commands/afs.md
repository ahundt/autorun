# AutoFile Strict Search Mode

Set file policy to strict search - only modify existing files, no new file creation.

## Usage

/afs

Activates strict search mode which blocks all new file creation and forces use of Glob/Grep to modify existing files only.